{% extends 'base.html.twig' %}

{% block title %}Room{% endblock %}

{% block body %}
    <h1>Chambre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ room.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ room.title }}</td>
            </tr>
            <tr>
                <th>Habitat</th>
                <td>{{ room.hotelid }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ room.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ room.image }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ room.price }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ room.status }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ room.number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_room_index') }}">Retourner</a>

    <a href="{{ path('admin_room_edit', {'id': room.id}) }}">modfier</a>

    {{ include('admin/room/_delete_form.html.twig') }}
{% endblock %}
